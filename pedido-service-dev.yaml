spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/pedidosdb?useSSL=false&serverTimezone=UTC
    username: devuser
    password: devpass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
